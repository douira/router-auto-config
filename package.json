{
  "name": "router-auto-config",
  "version": "1.0.0",
  "description": "can configure routers by sending requests to their web interfaces",
  "main": "main.js",
  "dependencies": {
    "colors": "^1.1.2",
    "object.values": "^1.0.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "douira",
  "engines": {
    "node": ">=6.9.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/douira/router-auto-config"
  }
}
